.panel.panel-default
  .panel-heading
    .panel-title.col-md-4= user_orders_headers(@current_user)
    .col-md-offset-8= link_to  'Change Order', new_user_order_path(@current_user.user_id), :class => "btn btn-default"
  .panel-body
    - if !@orders.empty?
      %table.table.table-bordered
        %tr
          %th Product
          %th= @orders.last.delivery_date
        - @orders.each do |order|
          %tr
            - if order.product.nil?
              %td &nbsp;
              %td &nbsp;
            - else
              %td= order.product.name
              %td
                = pluralize_units(order.quantity, order.product)
    - else
      %p
        No Orders found